{
  "name": "family-medical-record-backend",
  "version": "1.0.0",
  "description": "Backend for Family Medical Record System",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "npm install && npm install --prefix backend",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
